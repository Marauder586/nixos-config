---
description: Web research with citations.
agent: researcher
---

# /research $ARGUMENTS

Research: **$ARGUMENTS**

Use the SearXNG MCP first, fall back to DuckDuckGo and `fetch` for individual
URLs, escalate to Playwright only if a page requires JS execution.

Deliverable: a 5-15 line briefing with inline citations and a Sources
section listing all URLs visited.
